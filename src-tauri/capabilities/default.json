{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults, including event listen for scan-progress",
  "windows": ["main"],
  "permissions": ["core:default"]
}
